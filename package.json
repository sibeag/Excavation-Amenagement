{
  "name": "espace-sb-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --persist-to /tmp/ensemblesb-wrangler-state",
    "test": "node --test worker.test.mjs analytics.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
